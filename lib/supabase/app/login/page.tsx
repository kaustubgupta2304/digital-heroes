'use client';
import {useState} from 'react';import {supabaseBrowser} from '../../lib/supabase';import Link from 'next/link';import {useRouter} from 'next/navigation';
export default function Login(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();setError('');const {error}=await supabaseBrowser().auth.signInWithPassword({email,password});if(error)return setError(error.message);const {data}=await supabaseBrowser().auth.getUser();if(data.user){const {data:p}=await supabaseBrowser().from('profiles').select('role').eq('id',data.user.id).single();router.push(p?.role==='admin'?'/admin':'/dashboard');}}return <main className="formwrap"><form className="formcard" onSubmit={submit}><div className="eyebrow">Welcome back</div><h2 style={{font:'700 34px Space Grotesk'}}>Log in</h2>{error&&<div className="error">{error}</div>}<div className="field"><label>Email</label><input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></div><div className="field"><label>Password</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></div><button className="btn primary" style={{width:'100%'}}>Continue</button><p className="muted" style={{fontSize:13}}>New here? <Link href="/signup" style={{color:'var(--green)',fontWeight:700}}>Create an account</Link></p></form></main>}
