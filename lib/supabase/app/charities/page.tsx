'use client';
import {useEffect,useState} from 'react';import {supabaseBrowser} from '../../lib/supabase';
export default function Charities(){const [items,setItems]=useState<any[]>([]);const [q,setQ]=useState('');useEffect(()=>{supabaseBrowser().from('charities').select('*').order('featured',{ascending:false}).then(({data})=>setItems(data||[]))},[]);const filtered=items.filter(x=>(x.name+' '+x.description).toLowerCase().includes(q.toLowerCase()));return <main className="section"><div className="container"><div className="eyebrow">Give back</div><h1 style={{font:'700 60px Space Grotesk',letterSpacing:'-.06em',margin:'8px 0'}}>Causes worth showing up for.</h1><p className="lead">Explore the causes available to Digital Heroes members. Choose one during signup and send at least 10% of your membership to it.</p><div style={{margin:'25px 0'}}><input className="field input" style={{padding:13,width:'100%',border:'1px solid var(--line)',borderRadius:12}} placeholder="Search charities…" value={q} onChange={e=>setQ(e.target.value)}/></div><div className="charitygrid">{filtered.map(c=><div className="card" key={c.id}><div className="charityimg">{c.name.split(' ').slice(0,2).map((x:string)=>x[0]).join('')}</div><span className="pill" style={{marginTop:14}}>{c.featured?'Featured':'Charity'}</span><h3>{c.name}</h3><p className="muted">{c.description}</p>{c.events&&<p style={{fontSize:12,color:'var(--green)',fontWeight:700}}>Upcoming: {c.events}</p>}</div>)}</div></div></main>}
